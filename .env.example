# OCI Configuration
# Profile name from ~/.oci/config (defaults to "DEFAULT")
# Available profiles: DEFAULT, default, emdemo
# To switch profiles, change the value below and restart the MCP server
OCI_CLI_PROFILE=DEFAULT

# OCI Region (optional, overrides region in config file)
OCI_REGION=us-ashburn-1

# Default Compartment ID (optional, defaults to tenancy root)
# OCI_COMPARTMENT_ID=ocid1.compartment.oc1..aaaaaaaxxxxx

# ============================================================================
# Shared Cache (file or Redis)
# ============================================================================
# MCP_CACHE_DIR=~/.mcp-oci/cache
# MCP_CACHE_BACKEND=file
# MCP_REDIS_URL=redis://localhost:6379
# MCP_CACHE_KEY_PREFIX=mcp:cache

# Cache Compartment IDs (required for build_cache.py)
# Comma-separated list of root compartment OCIDs to cache
# The script will recursively scan all child compartments
# CACHE_COMPARTMENT_IDS=ocid1.compartment.oc1..aaa,ocid1.compartment.oc1..bbb

# ============================================================================
# Profile Selection Examples
# ============================================================================
# To use the emdemo profile:
# OCI_CLI_PROFILE=emdemo
#
# To use the default profile:
# OCI_CLI_PROFILE=default
#
# To use the DEFAULT profile (default):
# OCI_CLI_PROFILE=DEFAULT

# ============================================================================
# Region Examples
# ============================================================================
# OCI_REGION=us-phoenix-1
# OCI_REGION=eu-frankfurt-1
# OCI_REGION=ap-tokyo-1
# OCI_REGION=us-ashburn-1

# ============================================================================
# OpenTelemetry (optional)
# ============================================================================
# Enable/disable tracing
# OTEL_TRACING_ENABLED=true
#
# OCI APM (Production)
# OCI_APM_ENDPOINT=[Link to Secure Variable: OCI_APM_ENDPOINT]
# OCI_APM_PRIVATE_DATA_KEY=[Link to Secure Variable: OCI_APM_PRIVATE_DATA_KEY]
# OTEL_DISABLE_LOCAL=true
#
# Generic OTLP endpoint (HTTP or gRPC)
# OTEL_EXPORTER_OTLP_ENDPOINT=localhost:4317
